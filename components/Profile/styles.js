import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    rootView: {
      position: 'relative'
    },
    gravatar: {
      width: 50,
      height: 50,
      borderRadius: 10
    }
  });